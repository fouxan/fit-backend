# --- App ---
APP_NAME=Fit API
VERSION=0.1.0
DEBUG=true
ENV=development

# --- Database (Supabase Postgres) ---
# In Supabase -> Project Settings -> Database -> Connection Info
# Use +asyncpg for async SQLAlchemy; keep sslmode=require.
DATABASE_URL=postgresql+asyncpg://postgres.YOUR_USER:YOUR_PASSWORD@YOUR_HOST:5432/postgres?sslmode=require
# If you also run Alembic sync migrations, you may want a sync URL:
# DATABASE_SYNC_URL=postgresql://postgres.YOUR_USER:YOUR_PASSWORD@YOUR_HOST:5432/postgres?sslmode=require

# --- Security ---
SECRET_KEY=generate_a_long_random_string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- CORS ---
# Comma-separated; no brackets required in your validator:
BACKEND_CORS_ORIGINS=http://localhost:3000,https://your-frontend.example

# --- Google OAuth2 ---
# Google Cloud Console -> Credentials -> OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Make sure your authorized redirect URIs include your backend route, e.g.:
# http://localhost:8000/api/auth/google/callback
# https://api.yourdomain.com/api/auth/google/callback

# --- Email (choose provider) ---
# Gmail (workspace) example: create an App Password or use a provider key
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=fouzanfouzan1310@gmail.com
MAIL_PASSWORD="rgon zbii ellg kibo"
MAIL_FROM=you@yourdomain.com
MAIL_FROM_NAME=Fit App
MAIL_USE_CREDENTIALS=true
MAIL_STARTTLS=true
MAIL_SSL_TLS=false
MAIL_VALIDATE_CERTS=true
MAIL_TIMEOUT=15
MAIL_SUPPRESS_SEND=false    # true in tests/dev if you want to render but not send

# --- Redis ---
REDIS_URL=redis://localhost:6379

# --- Stripe ---
# Stripe Dashboard -> Developers -> API keys & Webhooks
STRIPE_SECRET_KEY=sk_live_or_test_xxx
STRIPE_PUBLISHABLE_KEY=pk_live_or_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# --- AWS / S3 (for your catalog media, etc.) ---
# IAM User or Role with s3:PutObject/GetObject; region from your bucket
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...
S3_EXERCISE_CATALOG_BUCKET=your-bucket-name

# --- Frontend URL (for links in emails) ---
FRONTEND_URL=http://localhost:3000

# --- OpenAI ---
# Platform: https://platform.openai.com -> API keys / Org / Project
OPENAI_API_KEY=sk-...
OPENAI_ORG_ID=org_...             # optional
OPENAI_PROJECT_ID=proj_...        # optional
OPENAI_MODEL=gpt-4o-mini          # default model your code will call

# --- (Optional) Supabase client values (if using Supabase client SDK) ---
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...
SUPABASE_DB_HOST=YOUR_HOST
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USER=postgres.YOUR_USER
SUPABASE_DB_PASSWORD=YOUR_PASSWORD
